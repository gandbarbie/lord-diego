# Audio Assets

Current configured background track:

`./assets/audio/hotel-lobby-colors-show-fast.mp3`

The page music toggle controls this file.

To change the song:
1. Put your new track in this folder.
2. Update the `<source src=\"...\">` inside `lord-diego-v2.html` (`#bg-drill-audio`).
